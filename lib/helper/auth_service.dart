class AuthService {

  Future signIn() async {
    try {
      // return await _auth.signOut();
    } catch (e) {
      print(e.toString());
      return null;
    }
  }
  
  Future signOut() async {
    try {
      // return await _auth.signOut();
    } catch (e) {
      print(e.toString());
      return null;
    }
  }
}
